export const selectMerchantProducts = (merchants, filters) => {
  // NOTE this is a stub to indicate how sorting and filtering
  // would be implemented,
  // i.e the `filters` object would be iterated over for
  // each of the merchant products.
  // The object would contain the property and match criteria
  // which would then be compared to the product data
  //
  if (!merchants || !filters) return merchants;
  return merchants;
};
