import * as React from "react";

import LogoutButton from "../../LogoutButton";

export const Auth: React.FC = () => {
  return (
    <div>
      <LogoutButton />
    </div>
  );
};

export default Auth;
