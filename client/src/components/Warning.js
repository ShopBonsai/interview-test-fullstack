import React from 'react';
import { Alert } from 'reactstrap';

const Warning = (props) => {
  return (
    <div>
      <Alert color="warning">
        0 results!
      </Alert>
    </div>
  );
};

export default Warning;